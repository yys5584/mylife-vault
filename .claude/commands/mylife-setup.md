---
description: MYLIFE vault 인터뷰 — 성격·환경·금전·직업·사주를 채워서 인생 OS 완성
---

# /mylife-setup

이 슬래시 커맨드를 실행하면 사용자에게 순서대로 질문해서 `templates/` 안 8개 마크다운을 채운다.

## 실행 순서

1. **현재 상태 진단**
   - `templates/` 디렉터리 안 모든 `.md` 파일을 빠르게 grep해서 `{{` placeholder 개수 카운트
   - 사용자에게 보고: "현재 X개 placeholder 비어있음. 어디부터 시작?"
   - 옵션 제시:
     - A) 처음부터 전체 인터뷰 (60-90분)
     - B) 섹션 선택 (필요한 것만)
     - C) 사주만 빠르게 (생년월일·시간만, 5분)

2. **인터뷰 진행 — `SETUP.md` 따르기**
   - `SETUP.md`에 8개 섹션 질문 시퀀스가 정의돼 있음
   - 한 섹션씩 진행. 한 섹션 끝나면 사용자에게 "다음 섹션 진행? 잠시 멈춤?" 확인
   - 사용자 답 받으면 즉시 해당 템플릿의 placeholder를 Edit으로 치환

3. **사주 + 별자리 계산 — 자동**
   - 섹션 0 (기본 정보) 끝나면 자동으로 두 스크립트 실행:
     - `scripts/calc_saju.py` → `templates/saju.md`
     - `scripts/calc_zodiac.py` → `templates/zodiac.md`
   - Python 없으면 → 만세력(원광대) + astro.com 안내 + 수동 입력 요청
   - **LLM이 사주 8자/행성 위치 직접 추정 ❌** — 항상 스크립트 또는 검증된 사이트 결과 사용

4. **인터뷰 톤**
   - 분석가 ❌, 코칭 파트너 ✓
   - 한 번에 1-2개 질문만. 답 받으면 다음.
   - 사용자가 "모름" / "넘어가" 하면 placeholder 그대로 두고 다음 질문
   - 답이 짧으면 후속 질문으로 살 붙임 (단, 질문 1개씩)

5. **저장 & 검증**
   - 매 섹션 끝날 때마다 해당 .md 파일 저장
   - 모든 섹션 끝나면 cross-reference 일치 확인:
     - `philosophy.md`의 정체성 한 문장 == `life_compass.md`의 정체성 한 문장
     - `self_profile.md`의 강점 == `life_compass.md`의 강점 표
     - `saju.md`의 약한 오행 → `life_os.md`의 보완 처방으로 연결됐나
   - 안 맞으면 사용자에게 알리고 통일 제안

6. **완료 메시지**
   - 채워진 파일 리스트 출력
   - "이제 어떤 LLM에서든 이 폴더를 컨텍스트로 받으면 너에 맞춰 답함."
   - 첫 사용 권유: "오늘 하나 자문해볼래? 큰 결정 / 매일 자문 / 충동 점검 중에서."

---

## 인터뷰 시퀀스 — 8개 섹션

상세 질문은 `SETUP.md`의 각 섹션 참조. 요약:

| # | 섹션 | 채우는 파일 | 예상 시간 | 필수? |
|---|---|---|---|---|
| 0 | 기본 정보 | self_profile.md (기본 정보 표) | 5분 | 필수 |
| 1 | 사주 + 별자리 계산 | saju.md, zodiac.md | 10분 (자동) | 필수 |
| 2 | 자기 진단 | self_profile.md (나머지) | 15분 | 필수 |
| 3 | 인생 철학 | philosophy.md | 20분 | 필수 |
| 4 | 시스템 처방 | life_os.md (본인 적용 섹션만) | 10분 | 필수 |
| 5 | 컴파스 + 로드맵 | life_compass.md, roadmap.md | 15분 | 필수 |
| 6 | 관계 프로토콜 | relationship_protocol.md | 20분 | 선택 (사건 있을 때만) |
| 7 | 사이드 프로젝트 | side_project_strategy.md | 15분 | 선택 (사이드 있을 때만) |
| 8 | 연애 스타일 | love_style.md | 25분 | 선택 (도메인 확장) |
| 9 | 투자 스타일 | investment_style.md | 25분 | 선택 (도메인 확장) |
| 10 | 직업 스타일 | career_style.md | 25분 | 선택 (도메인 확장) |

---

## 부분 갱신 모드

사용자가 이미 채워진 vault에서 `/mylife-setup`을 다시 실행하면:
- "전체 갱신? 특정 섹션만? 새 사건 추가?"
- 새 사건 = `relationship_protocol.md`에 새 섹션 추가, 기존 사건 보존
- 분기 갱신 = `roadmap.md` + `life_compass.md`의 분기 목표·5년 북극성 검토

## 분할 진행 가이드

깊은 자기성찰은 한 번에 30분 이상 못 함. AI가 분할 제안:
- **1차 ~30분 (가벼움)**: 섹션 0+1 (기본+자동) + 섹션 4 (시스템) — 최단 가치
- **2차 ~50분 (깊은 자기 진단)**: 섹션 2+3 (자기 + 철학) — 막히면 끊고 다음으로
- **3차 ~30분 + α**: 섹션 5 (컴파스·로드맵) 필수, 6·7 선택 (해당 시)

전체 시간: 필수만 ~110분 / 선택까지 ~140분.

각 차수 끝나면: "여기까지 저장. 다음 차수 언제 할래?" 묻고 종료.

## 자동 보강 (인터뷰 마지막)

수동 인터뷰 끝나면 AI가 자동으로:
1. **나이 자동 계산**: self_profile.md 생년월일 → 나이 채우기
2. **사주 ↔ 철학 매칭**: saju.md 일간 기질 + zodiac.md 태양궁 trait → philosophy.md *사주 기반 자기 이해* 섹션 자동 채우기
3. **빅3 ↔ 자기 진단 매칭**: 사주·별자리 강한 영역을 self_profile.md 강점 검증과 매칭, 코멘트 추가
4. **부족 영역 → 처방 매칭**: 사주 부족 오행 + 별자리 부족 원소를 life_os.md 6 레이어와 연결

---

## 인터뷰 마지막 — 사용자 안내

```
✅ 인터뷰 완료. 채워진 문서:
   - philosophy.md
   - self_profile.md
   - saju.md
   - life_os.md
   - life_compass.md
   - roadmap.md
   - (relationship_protocol.md / side_project_strategy.md는 해당 시)

이제 어떤 LLM에서든 이 폴더를 컨텍스트로 받으면 너에 맞춰 답해.

오늘 한 번 써볼래?
   1. 매일 자문 ("아침 자문 해줘" / "저녁 자문 해줘")
   2. 큰 결정 점검 ("이거 결정해도 될까?")
   3. 충동 점검 ("이 메시지 보낼까?")
```
